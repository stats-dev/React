import React from 'react'

const NotFound = () => {
  return (
    <div style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: 64,
        width: '100%',
        height: '100%'
    }}>NotFound</div>
  );
};

export default NotFound